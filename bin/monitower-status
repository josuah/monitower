#!/bin/sh -eu

spool=/var/spool/monitower

if [ "$#" = 0 ]; then
	for host in "$spool/"*; do
		set -- "$@" "${host##*/}"
	done
fi

for path in "$spool/"*"/"*"/current"; do
	echo
	echo "${path#$spool/}"
	monitower-show "$path"
done
echo
