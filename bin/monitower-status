#!/bin/sh -eu

base=$(cd "${0%/*}/.."; pwd)

for path in /var/spool/monitor/*/current; do
	[ -f "$path" ] || continue

	host=${path%/current}
	host=${host##*/}

	echo
	echo "$host"

	monitor-show "$path"
done
echo
