#!/bin/sh -eu
for x; do export "var_$x"; done

: ${var_time:=30}

q="
	SELECT ID FROM INFORMATION_SCHEMA.PROCESSLIST
		WHERE Time > $var_time
	AND	Command != 'Sleep'
"

exec [ -z "$(exec mysql -Be "$q")" ]
